
    <style>
        *_ {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body_ {
            background: linear-gradient(135deg, #1a2a6c, #b21f1f, #fdbb2d);
            background-size: 400% 400%;
            animation: gradient 15s ease infinite;
            color: #333;
            line-height: 1.6;
            min-height: 100vh;
            padding: 20px;
        }

        @keyframes gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .about-page {
            max-width: 1200px;
            margin: 0 auto;
            padding: 30px;
        }

        .container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
            padding: 40px;
            position: relative;
            overflow: hidden;
        }

        .container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: linear-gradient(90deg, #1a2a6c, #b21f1f, #fdbb2d);
        }

        h1 {
            text-align: center;
            font-size: 3rem;
            margin-bottom: 20px;
            color: #1a2a6c;
            position: relative;
            display: inline-block;
            left: 50%;
            transform: translateX(-50%);
        }

        h1::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 10%;
            width: 80%;
            height: 4px;
            background: linear-gradient(90deg, transparent, #1a2a6c, transparent);
        }

        hr {
            border: none;
            height: 2px;
            background: linear-gradient(90deg, transparent, #1a2a6c, transparent);
            margin: 30px 0;
        }

        .intro {
            font-size: 1.3rem;
            text-align: center;
            margin-bottom: 40px;
            color: #444;
            font-weight: 500;
            padding: 0 20px;
        }

        .content-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin: 40px 0;
        }

        .feature-card {
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border-top: 4px solid #1a2a6c;
            position: relative;
            overflow: hidden;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
        }

        .feature-card:nth-child(2) {
            border-top-color: #b21f1f;
        }

        .feature-card:nth-child(3) {
            border-top-color: #fdbb2d;
        }

        .feature-icon {
            font-size: 2.5rem;
            margin-bottom: 15px;
            color: #1a2a6c;
        }

        .feature-card:nth-child(2) .feature-icon {
            color: #b21f1f;
        }

        .feature-card:nth-child(3) .feature-icon {
            color: #fdbb2d;
        }

        .feature-title {
            font-size: 1.5rem;
            margin-bottom: 15px;
            color: #1a2a6c;
        }

        .feature-card:nth-child(2) .feature-title {
            color: #b21f1f;
        }

        .feature-card:nth-child(3) .feature-title {
            color: #fdbb2d;
        }

        .tech-stack {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 30px;
            margin: 40px 0;
            text-align: center;
        }

        .tech-title {
            font-size: 1.8rem;
            margin-bottom: 25px;
            color: #1a2a6c;
        }

        .tech-icons {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 30px;
        }

        .tech-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            transition: transform 0.3s ease;
        }

        .tech-item:hover {
            transform: scale(1.1);
        }

        .tech-icon {
            font-size: 3rem;
            margin-bottom: 10px;
            color: #1a2a6c;
        }

        .tech-name {
            font-weight: 600;
            color: #444;
        }

        .mission {
            background: linear-gradient(135deg, #1a2a6c, #2c3e50);
            color: white;
            border-radius: 15px;
            padding: 40px;
            margin: 40px 0;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .mission::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0) 70%);
        }

        .mission-title {
            font-size: 2rem;
            margin-bottom: 20px;
            position: relative;
        }

        .mission-text {
            font-size: 1.2rem;
            line-height: 1.8;
            position: relative;
        }

        .cta {
            text-align: center;
            margin-top: 40px;
        }

        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #1a2a6c, #b21f1f);
            color: white;
            padding: 15px 40px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.2rem;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        .cta-button:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 25px rgba(0, 0, 0, 0.3);
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2.2rem;
            }
            
            .content-grid {
                grid-template-columns: 1fr;
            }
            
            .container {
                padding: 20px;
            }
        }
    </style>

    <div class="about-page">
        <div class="container">
            <h1>AZ APP PROXY</h1>
            <p class="intro">a lightweight reverse proxy designed to streamline the hosting and routing of multiple applications with dynamic control.</p>
            
            <div class="content-grid d-none">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-bolt"></i>
                    </div>
                    <h3 class="feature-title">Built on Modern Technology</h3>
                    <p>Built on Node.js, AzAppProxy leverages the power of Redbird to provide efficient and flexible application management.</p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-users"></i>
                    </div>
                    <h3 class="feature-title">For Professionals</h3>
                    <p>Our platform is tailored for developers and DevOps professionals who need a reliable solution for managing application traffic and hosting environments.</p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-sliders-h"></i>
                    </div>
                    <h3 class="feature-title">Powerful Features</h3>
                    <p>With AzAppProxy, you can easily route requests, manage multiple apps, and ensure seamless integration with your existing infrastructure.</p>
                </div>
            </div>
            
            <div class="tech-stack">
                <h2 class="tech-title">Technology Stack</h2>
                <div class="tech-icons">
                    <div class="tech-item">
                        <i class="fab fa-node-js tech-icon"></i>
                        <span class="tech-name">Node.js</span>
                    </div>
                    <div class="tech-item">
                        <i class="fas fa-code tech-icon"></i>
                        <span class="tech-name">Express</span>
                    </div>
                    <div class="tech-item">
                        <i class="fas fa-feather tech-icon"></i>
                        <span class="tech-name">Handlebars</span>
                    </div>
                    <div class="tech-item">
                        <i class="fas fa-shield-alt tech-icon"></i>
                        <span class="tech-name">Redbird</span>
                    </div>
                </div>
            </div>
            
            {{!-- <div class="mission">
                <h2 class="mission-title">Our Mission</h2>
                <p class="mission-text">Join us on our mission to simplify application hosting and routing. Thank you for choosing AzAppProxy as your trusted solution for multi-app hosting.</p>
            </div> --}}
            
            {{!-- <div class="cta">
                <a href="#" class="cta-button">Get Started with AzAppProxy</a>
            </div> --}}
        </div>
    </div>

    <script>
        // Add subtle animations to feature cards on scroll
        document.addEventListener('DOMContentLoaded', function() {
            const featureCards = document.querySelectorAll('.feature-card');
            
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                    }
                });
            }, { threshold: 0.1 });
            
            featureCards.forEach(card => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';
                card.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
                observer.observe(card);
            });
            
            // Add hover effect to tech icons
            const techItems = document.querySelectorAll('.tech-item');
            techItems.forEach(item => {
                item.addEventListener('mouseenter', function() {
                    this.style.transform = 'scale(1.1)';
                });
                
                item.addEventListener('mouseleave', function() {
                    this.style.transform = 'scale(1)';
                });
            });
        });
    </script>